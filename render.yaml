services:
  - type: web
    name: security-scanner
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -t 65 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: MAX_SCAN_TIME
        value: "60"
